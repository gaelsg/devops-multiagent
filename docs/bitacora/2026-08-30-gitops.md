# 2026-08-30 — Jaeger migrado a k3s vía GitOps (Idea 9, post-roadmap)

Parte de la Idea 9 (ver `observability/docs/29110/idea9-gitops/` para el detalle completo). Esta entrada es solo el lado de este repo.

## Cambio
`OTEL_EXPORTER_OTLP_ENDPOINT` en `.env`/`.env.example`: `http://192.168.8.90:4317` → `http://192.168.8.92:4317`. Jaeger dejó de correr en el LXC `observability` (Docker Compose) y ahora corre en el cluster k3s, gestionado por ArgoCD.

## Verificado
Trace real (`uv run devops-agent diagnose "..."`) llegando a la nueva ubicación, confirmado vía la API de Jaeger (`/api/services` mostrando los 5 servicios esperados).
